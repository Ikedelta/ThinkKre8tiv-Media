{
  "buildCommand": "cd ../.. && corepack enable && corepack yarn workspace web build",
  "installCommand": "cd ../.. && corepack enable && corepack yarn install --immutable",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
